extends base
block append head
    //- script(src='/account/account.js')

block bodyContent
    .profile-container
        .container-tabs
            h2
                a.tabs(href='/account/profile') My Account
            h2
                a.tabs(href='/account/orders') orders &amp; returns
            h2
                a.tabs(href='/account/payment-info') payment info
            h2
                a.tabs(href='/account/shipping-info') shipping info
            h2
                a.tabs.active-tab(href='/account/wishlist') wishlist
            h2
                a.tabs(href='/account/email-preferences') email preferences
            h2
                a.tabs(href='/account/reset-password') password
        .container-user-info(style='width: 60%; margin: 0 auto')
            h2#reset-pass-holder(style='text-align: center') wishlist
            .wish-list-grid
                .wish-list-item.smaller(data-wishlist-item='0')
                    img.wish-list-image(src='/Images/Models/Filler/filler-bras-1.jpeg' alt='' srcset='')
                    h4
                        span#move-to-cart.text-btn move to cart
                        |  |
                        span#delete-wish-list.text-btn  remove
                    h2.wish-title Cotton Spring Lorem Lorem
                    h3 $25
