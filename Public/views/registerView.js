class RegisterView {
  constructor() {
    this._parentElement = document.querySelector('#register-form');
    // this.data = new formData(this._parentElement);
  }
}

export default new RegisterView();
